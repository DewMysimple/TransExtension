---
type: log
status: archived
kind: maintenance
importance: high
updated: 2026-09-10
topic: trans-extension-initialize-memory-and-git
project: trans-extension
affected_projects: [figma-zh-ui, github-zh-ui]
source_logs: []
supersedes: null
---

# 2026-09-10｜初始化统一工程记忆与 Git 仓库

- 时间：2026-09-10 23:42:28（北京时间，UTC+08:00）
- 类型：`maintenance`
- 项目：`trans-extension`
- 状态：`完成`
- 目标：建立根级统一工程记忆、分层 Agent 规则和根 Git 仓库，并完成首次远程交付。
- 日志索引：[[日志/MOC_工作日志|工作日志 MOC]]

## 已确认的决策

- 工程记忆只在仓库根维护一套，两个插件不复制记忆树。
- 根和两个插件使用 Codex/框架标准的复数文件名 `AGENTS.md`；子规则继承根规则。
- 所有受管记忆页以 `project` 标记主归属，跨插件页面补充 `affected_projects`。
- 完整对话产生仓库修改后，必须在结束前记忆同步、提交并推送；无修改时不创建空提交。

## 检查与操作

- 完整阅读桌面“工程记忆构建”框架的协议、理念、模板、状态页、索引说明和检查工具。
- 审计两个插件的 README、manifest、package、源码职责、测试、生成物、忽略规则与发布脚本。
- 确认根与子目录原先均无 Git 元数据，远程仓库无已有引用，可建立单一根仓库。
- 建立根 README、共享 AGENTS、两个局部 AGENTS、当前状态、ADR、知识页、模板、日志、索引与适配后的检查工具。
- 配置 `origin` 为 `https://github.com/DewMysimple/TransExtension.git`，默认分支为 `main`。

## 文件变更

- 新增根 `.gitignore`、`.gitattributes`、`README.md`、`AGENTS.md` 和 `llm-wiki.md`；统一文本行尾策略，PowerShell 脚本保留 CRLF。
- 新增 `figma-zh-ui/AGENTS.md` 与 `github-zh-ui/AGENTS.md`；两个既有插件 README 未删除或改写。
- 新增根级 `当前状态/`、`决策/`、`知识/`、`日志/`、`模板/` 和 `工具/memory_lint.py`。
- 适配 Front Matter、项目归属校验、文件链接校验、active 主题唯一性和 MOC 项目列。

## 测试与验证

- `figma-zh-ui`：`npm test` 6/6 通过。
- `github-zh-ui`：`npm test` 6/6 通过。
- 两个插件的 `npm run verify` 均仅因 `generated/sources.json` 超过 24 小时时效门槛失败；未为本次治理任务刷新上游。
- 工程记忆索引与 lint 在最终提交前刷新并检查；Python 编译检查和链接、严格日期、来源路径、替代关系、MOC 陈旧检测的内存边界测试通过。
- Git diff、忽略规则、本地提交和远程分支在交付前核对。

## 待确认长期记忆

- 无；用户明确要求的长期规则已在本轮作为 active 状态、决策和知识页落地。

## 问题、结果与下一步

- 结果：统一工程治理与记忆结构已建立，两个插件仍保持独立产品文档和运行边界。
- 遗留问题：两个插件的来源元数据已过 24 小时门槛，发布前需要按各自流程刷新并重新验证。
- 下一步：下一次插件功能或发布任务按所属 `project` 写入同一日志目录，并执行完整对话 Git 闭环。
