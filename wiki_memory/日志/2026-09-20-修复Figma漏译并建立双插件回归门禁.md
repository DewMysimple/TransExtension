---
type: log
status: archived
kind: bug
importance: high
updated: 2026-09-20
topic: trans-extension-translation-coverage-and-regression-gates
project: trans-extension
affected_projects: [figma-zh-ui, github-zh-ui]
source_logs: []
supersedes: null
---

# 2026-09-20｜修复 Figma 漏译并建立双插件回归门禁

- 状态：已完成代码修复、工程检查及交付准备；登录真实页面验收尚未完成。
- 目标：全面排查 Figma 多场景漏译，同时降低两个独立扩展继续修改时的回归风险。
- 日志索引：[[日志/MOC_工作日志|工作日志 MOC]]。

## 已确认的决策

- 用户明确要求先全面排查多个 Figma 场景，并统筹两个扩展的可维护性。
- 延续根与子项目既有规则：离线翻译、最小权限、保护用户内容、独立安装和打包；完成修改后记忆同步并提交推送。
- 没有获得真实页面逐项验收结果，不以词库数量、自动测试或合成页面结果声称全站完全翻译。

## 检查与操作

- 读取根及双插件规则、当前状态和相关模块/流程/决策；初始工作区干净。
- 并行检查 Figma 核心覆盖、GitHub 生命周期与审计、共同工程门禁，再交叉审查具体回归。
- 固定 SHA 上游只有一个翻译数据文件，未发现遗漏导入其他词库；上游内容脚本明确处理普通面板的 `translate="no"` 和拆分 `i18n-text`。
- 结合 Figma 官方页面核对文件夹、Design、FigJam、Slides、设置和管理等固定标签，参考来源随回归 fixture 保存。
- 没有刷新上游、修改生成词库、许可证、版本或浏览器权限；没有创建发布 ZIP。

## 文件变更

- `figma-zh-ui/src/core.js`：词条内部空白规范化、规范化冲突保护、覆盖优先级、预编译完整模板匹配、模板具体度选择、捕获值单次替换、已知保留英文标记。
- `figma-zh-ui/src/official-overrides.js` 与 `tests/fixtures/figma-ui-coverage.json`：补充 25 条人工词条，建立 7 类场景、53 个固定标签的真实交付词库回归，并记录官方来源。
- `figma-zh-ui/src/content.js`：固定控件提示属性、普通面板 `translate="no"` 受限例外、纯展示 `i18n-text` 整句处理、SPA 全页重扫、移除/复用/进入保护区后的原文恢复、过期提示清理、拖拽/可编辑/文件夹/工作区名称保护、发送前重新核验审计来源。
- `figma-zh-ui/src/background.js` 与测试：复用核心候选过滤，限定审计类别，停用时拒收排队批次，串行存储回归。
- `github-zh-ui/src/core.js`、`content.js`、`background.js`、`popup.*` 与测试：修复路由漏扫、悬停原文自翻译、失效记录/节点恢复、动态保护和输入类型变化；规范审计类别，发送前核验来源，停止审计时释放队列；迁移旧审计路径并清洗读取及导出，保留合法计数和时间。
- 双插件 `scripts/verify-project.mjs`、`package.json`：新增 `verify:offline`，仍检查权限/域/加载顺序/版本/文件/来源结构/语法/常见联网调用；默认 `verify` 继续保留 24 小时来源门槛。
- 根 `package.json`、`scripts/check-projects.mjs`、`tests/verification.test.mjs`、`.github/workflows/checks.yml`：统一检查入口及 Windows/Linux Node.js 22 CI；两个插件运行时仍不依赖根层。
- `scripts/browser-smoke.mjs`：独立临时浏览器配置与本地合成页面，加载真实打包词库和内容脚本，Chrome API 使用测试替身。
- 根及双插件 README、`docs/maintenance.md`：修复分类、职责边界、正反回归、源码重新加载方法、真实页面验收矩阵及发布限制。

## 测试与验证

- 最终根 `npm run check` 通过：共享门禁 32 项、Figma 43 项、GitHub 31 项，共 106 项自动测试；双插件 `verify:offline` 和记忆检查通过。
- 本机 Chrome 合成页面冒烟通过：Figma 12 项、GitHub 10 项，覆盖固定控件、用户内容保护、异步节点、路由、恢复和取消审计。
- 本机 Edge 合成启动未返回 DOM 验证结果，不能计为通过；没有操作正在使用的浏览器会话。
- 两个默认 `npm run verify` 都仅因来源超过 24 小时失败；遵循既有流程保留固定上游版本，没有为了通过检查刷新来源。
- `git diff --check` 通过；提交前 `git fetch origin` 确认当前 main 与 origin/main 未分叉。
- 没有执行登录后 Figma/GitHub 真实站点的 Chrome/Edge 人工验收，未声称 CI 已在远端运行通过。

## 待确认长期记忆

- 可将根 `npm run check` 作为日常共同质量入口，`verify:offline` 用于普通维护、默认 `verify` 用于发布；实现与使用说明已落地，本轮不新增 ADR。
- 可将“先判定缺词/上下文/拆分文案/生命周期，再补固定控件与同文用户内容的正反回归”确立为后续漏译维护规范。
- 对两个独立插件先统一验证契约和模块职责，不为去重而引入跨目录运行时依赖；后续是否提取共享实现需额外确认。

## 问题、结果与下一步

- 已修复可复现的漏译与生命周期缺陷，并为词库覆盖、用户内容保护、恢复和审计建立回归保障。
- 真实站点仍可能存在账号、功能版本和 DOM 差异；缺少实际漏译词条或脱敏结构时，不盲目扩大用户内容保护例外。
- 浏览器应加载本轮 `figma-zh-ui/` 和 `github-zh-ui/` 源码目录并重新加载扩展、刷新页面；旧 ZIP 不会自动更新。本轮询问了现有安装目录，封存时尚未得到答复。
- 下一步按子 README 完成登录真实页面验收，利用只含固定界面文字的本地漏译报告补充最小回归；准备发布时再刷新对应插件来源并完成发布验证和打包。
- 本轮文件与本日志一并纳入一个提交并推送；提交标识和推送结果在交付答复记录。
