{
  "name": "trans-extension",
  "private": true,
  "description": "Shared development checks for independent offline translation extensions",
  "scripts": {
    "check": "node scripts/check-projects.mjs",
    "test:tooling": "node --test tests/*.test.mjs",
    "check:memory": "python -X utf8 wiki_memory/工具/memory_lint.py check"
  },
  "engines": {
    "node": ">=22"
  }
}
