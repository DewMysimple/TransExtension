# LLM Wiki 模式在本仓库的落地

本文保留“工程记忆构建”框架的核心理念，解释 TransExtension 为什么采用当前结构。具体可执行规则以根 [`AGENTS.md`](./AGENTS.md) 为准。

## 核心思想

普通检索会在每次任务中从原始文件重新拼装上下文；工程记忆则由 Agent 持续维护一个结构化、可审计、会累积的 Markdown wiki。它不替代代码和文档，而是在二者之上保存已确认结论、关联和变更历史。

这个模式有三层：

1. **原始事实来源**：源码、配置、插件 README、测试、来源元数据和用户提供的资料。它们具有最高事实优先级。
2. **工程记忆**：`当前状态/`、`决策/`、`知识/` 和 `日志/`。Agent 负责建立链接、综合结论并保持当前状态一致。
3. **Schema 与流程**：根 `AGENTS.md` 和模板定义读取顺序、Front Matter、确认边界、索引和 Git 闭环。

## 为什么区分历史与现状

- `日志/` 追加记录每次实质任务发生了什么，便于审计，但不保证其中的所有描述仍然有效。
- `当前状态/` 只保存现在应该相信的事实，让新会话可以低成本启动。
- `决策/` 保存选择、理由、影响和替代关系，旧决策不会因过时而被删除。
- `知识/` 保存经过确认、可重复使用的模块、流程、规范和运维方法。

因此，查询当前行为时先读状态和知识；只有需要理解原因时才追溯决策与日志。

## 人机确认边界

Agent 可以自动记录本轮日志，但未经确认的新候选事实不能直接进入 active 长期记忆。用户在需求中明确提出的规则视为已确认，例如：统一根级工程记忆、每个插件使用局部 `AGENTS.md`、完整对话产生修改后必须提交并推送。

当新结论取代旧结论时，更新 active 页面，将旧页面标记为 `superseded` 或 `deprecated`，并保留来源链。这样既能快速获得当前答案，也不会丢失历史。

## 单一记忆、多项目归属

本仓库不为每个插件复制一套 wiki。所有记忆都在根目录维护，并使用 `project` 与可选的 `affected_projects` 区分根治理、Figma 插件和 GitHub 插件。日志 MOC 同时提供时间、类型和项目维度的导航。

这种做法保留了两个插件的独立产品边界，同时让公共隐私原则、验证流程、决策与 Git 历史只有一个权威入口。
